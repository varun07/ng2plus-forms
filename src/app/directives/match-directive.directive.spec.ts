import { MatchDirectiveDirective } from './match-directive.directive';

describe('MatchDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
